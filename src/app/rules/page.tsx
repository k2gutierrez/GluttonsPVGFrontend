import { Header } from '@/components/Header'; import { Panel,Kicker } from '@/components/Terminal';
const rules=[['01 // CLOCK','24H START / 36H CAP','Hungry at 12H or less. One real hour always equals one Glutton hour.'],['02 // FEED OR FAST','PAY OR TAKE THE RISK','Feed extends time. FAST is free and can keep you alive at 0H — but makes you vulnerable.'],['03 // POISON','MAKE SOMEONE HUNGRIER','Target another Glutton. A successful normal Poison cuts its remaining clock in half.'],['04 // DEATH = FOOD','BODIES STAY IN THE GAME','Death creates a Fresh corpse. Fresh can be eaten, preserved, or eventually turns Rotten.'],['05 // PHASES','THE WORLD GETS WORSE','Feast → Plague → Last Supper. New dangers unlock and normal food eventually disappears.']];
export default function Rules(){return <><Header/><main className="mx-auto max-w-[1500px] px-4 py-7 md:px-7"><Kicker>rules / machine spec</Kicker><div className="grid gap-4 lg:grid-cols-[1fr_300px]"><div><h1 className="font-display text-5xl uppercase md:text-7xl">Understand the machine in 60 seconds.</h1><p className="mt-3 max-w-4xl text-sm text-zinc-400">STAY ALIVE. HOWEVER YOU CAN. All minted Gluttons wake together on a real-time hunger clock. Keep yours alive while the shared Pot grows and the rules get harsher.</p></div><Panel className="p-5"><Kicker>the end</Kicker><p className="font-display text-2xl uppercase">One Glutton survives — or the final table settles under the contract rules.</p></Panel></div><div className="mt-6 grid gap-3 md:grid-cols-5">{rules.map(([k,h,p])=><Panel key={k} className="p-4"><div className="text-[9px] text-[#ff5b2e]">{k}</div><div className="mt-5 font-display text-2xl uppercase">{h}</div><p className="mt-4 text-[10px] leading-5 text-zinc-500">{p}</p></Panel>)}</div><Panel className="mt-5 p-5 md:p-7"><Kicker>how the game escalates</Kicker><div className="bar"><i style={{width:'74%'}}/></div><div className="mt-6 grid gap-3 md:grid-cols-3"><Phase n="01" name="THE FEAST" trigger="GAME START" actions="FEED / FAST / POISON / FRESH / ROTTEN / POWER"/><Phase n="02" name="THE PLAGUE" trigger="ALIVE ≤ 50% OR MEAL = +2H" actions="ALL FEAST ACTIONS + DEVOUR THE LIVING"/><Phase n="03" name="LAST SUPPER" trigger="ALIVE ≤ 2.5% OR DAY 120 + WARNING" actions="FRESH / ROTTEN / DEVOUR / TRUCE"/></div></Panel><Panel className="mt-5 p-5"><Kicker>money moves the same machine</Kicker><div className="grid gap-4 md:grid-cols-5"><Money a="MINT" b="0.004 APE" c="95% POT / 5% PVG"/><Money a="FEED" b="0.0006 APE" c="92% POT / 8% PVG"/><Money a="POISON" b="0.0004 APE" c="92% POT / 8% PVG"/><Money a="POWER" b="0.0003 APE" c="92% POT / 8% PVG"/><Money a="ROYALTY" b="4%" c="3.5% POT / 0.5% PVG"/></div></Panel></main></>}
function Phase({n,name,trigger,actions}:{n:string,name:string,trigger:string,actions:string}){return <div className="border border-white/10 p-5"><div className="text-[9px] text-[#ff5b2e]">{n}</div><div className="mt-3 font-display text-3xl">{name}</div><div className="mt-6 text-[9px] text-zinc-600">TRIGGER</div><div className="mt-2 text-xs text-[#ff5b2e]">{trigger}</div><div className="mt-5 text-[9px] text-zinc-600">ACTIONS</div><p className="mt-2 text-xs leading-6">{actions}</p></div>};function Money({a,b,c}:{a:string,b:string,c:string}){return <div className="border-r border-white/10 p-3"><div className="text-[9px] text-zinc-500">{a}</div><div className="mt-3 font-display text-3xl">{b}</div><div className="mt-2 text-[9px] text-[#ff5b2e]">{c}</div></div>}
