import { NextResponse } from 'next/server';import { isAddress,getAddress } from 'viem';import { getConsistentWallet } from '@/server/read-service';
export const runtime='nodejs';export const dynamic='force-dynamic';
export async function GET(_:Request,{params}:{params:Promise<{address:string}>}){try{const{address}=await params;if(!isAddress(address))return NextResponse.json({error:'BAD_ADDRESS'},{status:400});const snap=await getConsistentWallet(getAddress(address));if(!snap)return NextResponse.json({error:'INDEXER_BOOTSTRAPPING'},{status:503,headers:{'Cache-Control':'no-store'}});const{protocol,tokens}=snap;return NextResponse.json({protocol,tokens,walletBalance:tokens.length,indexedAt:protocol.indexedAt},{headers:{'Cache-Control':'public, max-age=2, s-maxage=4, stale-while-revalidate=15'}});}catch(e:any){return NextResponse.json({error:e?.message||'READ_SERVICE_UNAVAILABLE'},{status:503,headers:{'Cache-Control':'no-store'}});}}
