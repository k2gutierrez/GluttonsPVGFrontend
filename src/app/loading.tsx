export default function Loading(){return <main className="page-shell"><section className="panel protocol-hard-sync"><div className="kicker">terminal loading</div><h1>READING THE TABLE.</h1><p>Loading interface modules. Canonical state will appear only after the shared read layer responds.</p><div className="endgame-sync-line"><i/><span>LOADING UI · NO STATE GUESSES</span></div></section></main>}
