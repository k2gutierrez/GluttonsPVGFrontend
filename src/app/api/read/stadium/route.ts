import { NextResponse } from 'next/server';import { compactStadium } from '@/server/read-service';
export const runtime='nodejs';export const dynamic='force-dynamic';
export async function GET(){try{const x=await compactStadium();if(!x)return NextResponse.json({error:'INDEXER_BOOTSTRAPPING'},{status:503,headers:{'Cache-Control':'no-store'}});return NextResponse.json(x,{headers:{'Cache-Control':'public, max-age=5, s-maxage=20, stale-while-revalidate=120','X-Gluttons-Stadium-Block':String(x.stadiumBlock)}});}catch(e:any){return NextResponse.json({error:e?.message||'READ_SERVICE_UNAVAILABLE'},{status:503,headers:{'Cache-Control':'no-store'}});}}
